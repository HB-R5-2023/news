{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}
	- Accueil
{% endblock %}

{% block body %}
	<h1>ARTICLES</h1>

	<div>
		{% for article in articles %}
			<p>
				<a href="{{ path("app_article", { "id": article.id }) }}">{{ article.title }}</a>
			</p>
		{% else %}
			<div>Aucun article trouvé</div>
		{% endfor %}
	</div>
{% endblock %}
